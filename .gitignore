# compilation artifacts
out/

# object files
*.o

os-libc/linked-list/demo-singly-linked-list*
!os-libc/linked-list/demo-singly-linked-list.zig

# ELF files
*.elf

# stuff I don't want to commit, or that I still don't know where to place in the project
wip/
